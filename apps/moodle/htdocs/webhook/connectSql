<?php
// open sql
$con = mysql_connect("localhost","root","1234qwer");
if (!$con)
  {
  die('Could not connect: ' . mysql_error());
  }

// select md_user
mysql_select_db("md_user", $con);

// get all data
while($row = mysql_fetch_array($result))
  {
  echo $row['FirstName'] . " " . $row['LastName'];
  echo "<br />";
  }



// close sql

mysql_close($con);
?>
